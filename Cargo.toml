[package]
name = "gitilante"
version = "0.1.0"
edition = "2024"
description = "A lightweight GUI for viewing Git and manipulating hunks"
rust-version = "1.85"

[dependencies]
env_logger = "0.11"
log = "0.4"

[profile.dev]
# Faster debug builds; only affects debug info.
debug = 1
